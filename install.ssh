#!/bin/bash

dir="system-engineering"
gitrepository="https://github.com/ss94460ss/SuperMegaBank"

mkdir -p "$dir"

cd $dir
git clone $gitrepository
find /A -type f -exec chmod 0777 {} \;
docker-compose up
